// write a program to print the sum of the first n odd numbers.

#include <stdio.h>

int main(){
    int n;
    printf("enter the value of n: ");
    scanf("%d",&n); //n= can be any first n number of odd numbers
    
    printf("the sum of the first n odd numbers is: %d\n", n*n );
    return 0;
}